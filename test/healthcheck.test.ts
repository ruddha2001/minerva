require("dotenv").config();
import chai from "chai";

const expect = chai.expect;

export const healthcheckTestCases = () => {
  describe("Health Check ", function () {
    it("Healthcheck should be dislaying uptime", () => {});
  });
};
